#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int num = 0;
	/* betty style doc for function main goes there */
	for (num; num < 10; num++)
	{
		printf("%d", num);
	}
	return (0);
}
