#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char letter = 'z';
	/* betty style doc for function main goes there */
	while (letter >= 'a')
	{
		putchar(letter);
		last--;
	}
	return (0);
}
